<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Style x:Key="StandartText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="TitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="37"/>
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="HoverText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#007ACC"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Background" Value="#007ACC"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Bahnschrift SemiLight"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="6"/>
    </Style>


    <Style x:Key="SelectableHoverText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
        <Setter Property="FontWeight" Value="Normal"/>

        <!-- Триггер при наведении курсора -->
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#007ACC"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Trigger>

            <!-- Триггер при выборе элемента -->
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                <Setter Property="Foreground" Value="#007ACC"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- Custom Style for Calendar Day Button -->
    <Style x:Key="CustomCalendarDayButton" 
        TargetType="CalendarDayButton" 
        BasedOn="{StaticResource MaterialDesignCalendarDayButton}">
        <Setter Property="materialDesign:CalendarAssist.SelectionColor" Value="#007ACC" />
        <Setter Property="materialDesign:CalendarAssist.SelectionForegroundColor" Value="White" />
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
    </Style>

    <!-- Custom Style for Calendar Button -->
    <Style x:Key="CustomCalendarButton" 
        TargetType="CalendarButton" 
        BasedOn="{StaticResource MaterialDesignCalendarButton}">
        <Setter Property="materialDesign:CalendarAssist.SelectionColor" Value="#3DAB28" />
        <Setter Property="materialDesign:CalendarAssist.SelectionForegroundColor" Value="White" />
    </Style>

    <!-- Custom Style for Calendar -->
    <Style x:Key="CustomCalendar" 
        TargetType="{x:Type Calendar}" 
        BasedOn="{StaticResource MaterialDesignDatePickerCalendarPortrait}">
        <Setter Property="Background" Value="White" />
        <Setter Property="CalendarButtonStyle" Value="{StaticResource CustomCalendarButton}" />
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource CustomCalendarDayButton}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="materialDesign:CalendarAssist.HeaderBackground" Value="#3DAB28" />
        <Setter Property="materialDesign:CalendarAssist.HeaderForeground" Value="White" />
    </Style>

    <!-- Custom Style for Outlined DatePicker -->
    <Style x:Key="CustomOutlinedDatePicker" 
        TargetType="DatePicker" 
        BasedOn="{StaticResource MaterialDesignOutlinedDatePicker}">
        <Setter Property="CalendarStyle" Value="{StaticResource CustomCalendar}" />
        <Setter Property="FontFamily" Value="Cascadia Mono SemiLight"/>
    </Style>
    <!-- Определение цветов -->
    <Color x:Key="ComboBoxForegroundColor">Black</Color>
    <Color x:Key="ComboBoxUnderlineBrushColor">#3DAB28</Color>
    <Color x:Key="ComboBoxBackgroundColor">WhiteSmoke</Color>
    <Color x:Key="ComboBoxDisabledUnderlineBrushColor">LightGray</Color>

    <!-- Определение кистей -->
    <SolidColorBrush x:Key="ComboBoxForegroundBrush" Color="{StaticResource ComboBoxForegroundColor}" />
    <SolidColorBrush x:Key="ComboBoxUnderlineBrush" Color="{StaticResource ComboBoxUnderlineBrushColor}" />
    <SolidColorBrush x:Key="ComboBoxBackgroundBrush" Color="{StaticResource ComboBoxBackgroundColor}" />
    <SolidColorBrush x:Key="ComboBoxDisabledUnderlineBrush" Color="{StaticResource ComboBoxDisabledUnderlineBrushColor}" />

    <!-- Стиль ComboBox -->
    <Style x:Key="CustomComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
        <Setter Property="Foreground" Value="{StaticResource ComboBoxForegroundBrush}" />
        <Setter Property="Background" Value="{StaticResource ComboBoxBackgroundBrush}" />
        <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="{StaticResource ComboBoxUnderlineBrush}" />

        <!-- Настройка для отключенного состояния -->
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="{StaticResource ComboBoxDisabledUnderlineBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="ScrollBarRepeatButtonStyle" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Gray" CornerRadius="4"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Изменение скрола -->
    <!-- Кнопка со стрелкой вверх -->
<Style x:Key="RepeatButtonBaseStyle" TargetType="RepeatButton">
    <Setter Property="OverridesDefaultStyle" Value="True"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Background" Value="Gray"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="2"/>  </Style>

<Style x:Key="RepeatButtonUpStyle" TargetType="RepeatButton" BasedOn="{StaticResource RepeatButtonBaseStyle}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
                <Path Data="M 0,8 L 4,0 L 8,8 Z" Fill="White" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

<Style x:Key="RepeatButtonDownStyle" TargetType="RepeatButton" BasedOn="{StaticResource RepeatButtonBaseStyle}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
                <Path Data="M 0,0 L 4,8 L 8,0 Z" Fill="White" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

<Style x:Key="RepeatButtonLeftStyle" TargetType="RepeatButton" BasedOn="{StaticResource RepeatButtonBaseStyle}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
                <Path Data="M 8,0 L 0,4 L 8,8 Z" Fill="White" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

<Style x:Key="RepeatButtonRightStyle" TargetType="RepeatButton" BasedOn="{StaticResource RepeatButtonBaseStyle}">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="RepeatButton">
                <Path Data="M 0,0 L 8,4 L 0,8 Z" Fill="White" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

    <Style x:Key="RoundedScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track x:Name="PART_Track" Orientation="{Binding Orientation, RelativeSource={RelativeSource AncestorType=ScrollBar}}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton x:Name="PART_DecreaseButton" Style="{StaticResource RepeatButtonUpStyle}" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Background="DarkGray" Margin="2">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton x:Name="PART_IncreaseButton" Style="{StaticResource RepeatButtonDownStyle}" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Если ориентация горизонтальная, меняем стили кнопок -->
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_DecreaseButton" Property="Style" Value="{StaticResource RepeatButtonLeftStyle}"/>
                            <Setter TargetName="PART_IncreaseButton" Property="Style" Value="{StaticResource RepeatButtonRightStyle}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="RoundedScrollViewerStyle" TargetType="ScrollViewer">
    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="ScrollViewer">
                <Border CornerRadius="10"
                        BorderBrush="Gray"
                        BorderThickness="1"
                        Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="1"
                                   Grid.Row="0"
                                   Orientation="Vertical"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Style="{StaticResource RoundedScrollBarStyle}"/>
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Column="0"
                                   Grid.Row="1"
                                   Orientation="Horizontal"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Style="{StaticResource RoundedScrollBarStyle}"/>
                    </Grid>
                </Border>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
</Style>

</ResourceDictionary>